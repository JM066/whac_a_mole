{
  "roots": ["<rootDir>"],
  "testEnvironment": "jsdom",
  "moduleNameMapper": {
    "\\.(css|scss)$": "identity-obj-proxy",
    "^@/(.*)$": "<rootDir>/src/$1"
  },
  "setupFilesAfterEnv": ["<rootDir>/jest-setup.ts"],
  "transform": {
    "^.+\\.tsx?$": "ts-jest"
  }
}
